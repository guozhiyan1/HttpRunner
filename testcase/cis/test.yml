-   config:
        base_url: ${ENV(host)}
        name: 床位维护冒烟测试
        variables:
            k1: ${ENV(out_k1)}
            k2: ${ENV(in_k2)}
            orgId: ${ENV(orgId)}
            areaId: ${ENV(in_k3)}
            locationId: 99897828609
            master_patient_index: ${get_result_dicts(master_patient_index)}
            bed_number: ${get_result_dicts(bed_number)}
            bed_id: ${get_result_dicts(bed_id)}
            patient_id: ${get_result_dicts(patient_id)}
            order_id: ${get_result_dicts(order_id)}
            inhospital_id: ${get_result_dicts(inhospital_id)}

-   test:
        api: api/token.yml
        name: 登录获取token

-   test:
        api: api/setting.yml
        name: 登录设置

-   test:
        api: api/user_info.yml
        name: 切换登录院区部门


-   test:
        name: 查询某个病区的待安床病人列表
        t
#        -   eq:
#            - ${get_sql_result(query_inhospital_order_exec, master_patient_index=DUqE9m7bWKOdmVbwHCEqTkcVfdJ7ib2a, order_id=$order_id)}
#            - True
#
#
